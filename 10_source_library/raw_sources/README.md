# Raw Sources

Raw PDFs and scans are intentionally not stored here yet.

Reasons:

- files may be large
- copyright/provenance is unresolved
- git is not the right storage for large binary source corpora unless Git LFS or external storage is chosen

Use `../inventory/BOOK_LIST_SEED.md` and `../provenance/PROVENANCE_POLICY.md` first.

